export class Player {
  constructor(private name: string,
              private x: number,
              private y: number,
              private rotation: number,
              private gun_rotation: number) {
  }
}
